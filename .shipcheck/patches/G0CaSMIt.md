# No input validation library detected
Using Zod for input validation ensures that all external inputs are properly checked before use.
```typescript
import { z } from 'zod';

const inputSchema = z.object({
  name: z.string().min(1),
  email: z.string().email(),
});

const validatedInput = inputSchema.parse(inputData);
```
_Generated by ShipCheck — review before merge._