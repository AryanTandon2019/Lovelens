# No input validation library detected
Using Zod for input validation ensures that all external inputs are correctly validated before processing.
```typescript
import { z } from 'zod'; const schema = z.object({ name: z.string().min(1), email: z.string().email() }); const result = schema.safeParse(input);
```
_Generated by ShipCheck — review before merge._